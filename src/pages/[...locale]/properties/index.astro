---
/* /booking — 旧サイトでは選択ページではなくコンバージョンページ
   （「We are truly looking forward...」＋ Airbnb CTA×2）。旧 Booking.tsx を忠実移植 */
import SiteLayout from "../../../layouts/SiteLayout.astro";
import BookingConversion, { BK_HERO_PRELOAD } from "../../../components/BookingConversion.astro";
import { LOCALES, localeParam, type Locale } from "../../../i18n/config";
import { getPageMeta } from "../../../lib/seo";

export function getStaticPaths() {
  return LOCALES.map((locale) => ({
    params: { locale: localeParam(locale) },
    props: { lang: locale as Locale },
  }));
}

const { lang } = Astro.props as { lang: Locale };
const meta = await getPageMeta("booking", lang);
---

<SiteLayout lang={lang} path="/properties" title={meta.title} description={meta.description} preloadImage={BK_HERO_PRELOAD}>
  <BookingConversion lang={lang} />
</SiteLayout>
