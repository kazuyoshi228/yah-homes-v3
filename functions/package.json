{
  "name": "yah-homes-functions",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "test": "tsx --test src/agency/*.test.ts"
  },
  "dependencies": {
    "@google/genai": "^0.14.1",
    "@googleapis/gmail": "^18.0.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.1.0",
    "google-auth-library": "^11.0.0",
    "nodemailer": "^9.0.3",
    "stripe": "^22.4.0"
  },
  "devDependencies": {
    "@types/nodemailer": "^8.0.1",
    "tsx": "^4.23.12",
    "typescript": "^5.7.0"
  }
}
