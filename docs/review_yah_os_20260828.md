# yah.OS 全体レビュー（上級エンジニア視点・5観点並列精査）

- 状態: **P0全8件＋P1 修正済み・本番デプロイ済み（2026-08-28）**。P2は未着工（各カードスレッドへ指示書化予定）
  - P1実施内容: jobs起票の冪等化／intake=flash+responseSchema+要目視前置／askAIのモデル指定・メモ化・usage台帳(aiLogs)・打ち切り注記・429リトライ／READABLE+=construction・policies・mailFailures／朝メール所見flash化／月次レポ失敗時フォールバックメール／health4検査（売上重複・棟実在・日付欠測・現金鮮度）／api()堅牢化／alert→notify／稼働中フィルタ=warn常時表示／取消を詳細のみへ／未ログインバナー／質問窓の失敗時復元／preconnect+キャッシュ調整
  - P0検証: ポータル=スラッグ認可200・git露出旧トークン404・業者URL不変を実測確認。テスト17本＋スモーク21頁通過。
    追加発見: 業者ポータル3関数もCIデプロイ対象外だった（3度目の同型罠）→ CI追加済み
- 起票: 2026-08-28。発注者「ウェブサイト全体を見直して、改善点を提案。徹底的に」
- 方法: フロント性能／セキュリティ／バックエンドAPI／データ・AI基盤／UX の5並列レビュー（全て実ファイル・file:line根拠）。既定方針（ビルド無し・1カード1HTML・SSoT・小規模適正）内の提案のみ。

## 総評

設計規律（fail-closed・SSoT・リネージ・機械ゲート）は一貫して高評価。ただし**受信系の冪等性**と**AIレスポンスへの過信**、**ハードコードの時限爆弾**に確定バグが集中。うち4件は即日修正可能。セキュリティは全体に良好だが、**業者トークンのgit露出**と**通信費パスワードの平文配信**の2件が要対処。

## P0 — 確定バグ・要即修（承認後すぐ着工・計1.5〜2日）

| # | 内容 | 根拠 | 工数 |
|---|---|---|---|
| 1 | **朝メールのAI所見がHTML版に一度も載っていない**（挿入アンカーの文字列が現テンプレに存在せずreplace空振り。Gemini生成コストだけ発生） | alerts.ts:243 | 30分 |
| 2 | **月次レポートの年月が1月に壊れる**（月だけ戻して年を戻していない→2027-01送信分が「2027-12」表記） | functions.ts:350 | 15分 |
| 3 | **factsのinvest/add判定が文字列"2026"直書き**——2027年以降の追加投資が全て取得投資に誤分類され投資額・利回りが静かに狂う時限爆弾 | facts.ts:52,64 | 1時間（棟の取得完了日と比較へ） |
| 4 | **gmailPush: 1通の処理失敗でもhistoryIdを進めメールを恒久取りこぼし**＋受信系全経路に冪等性なし（Pub/Sub再配信で intake二重・最悪**業者へ確認メール二重送信**） | functions.ts:389-411, inbox.ts, dispatcher.ts | 1日 |
| 5 | **業者ポータルのトークンが firebase.json（git履歴に永久残存）に平文**＋URLクエリで流通。書込権限とチェックアウト日（空き家日＝物理防犯機微）を解錠する | firebase.json:59,64, portal.ts | 半日（即ローテ＋スラッグ→サーバ解決方式へ） |
| 6 | **通信費アカウントのパスワードが平文でAPI配信されsessionStorageに残存**（コメントの意図に実装が反している） | utilities.html:578,664 | 半日（一覧から除外・都度取得化） |
| 7 | メンテナンスの「台帳へ」ボタンが存在しないタブを呼び無反応 | maintenance.html:966（led→ren） | 5分 |
| 8 | acceptIntake二重クリックで業者二重登録（非トランザクション） | intake.ts:95-126 | 1時間 |

## P1 — 短期（今週・計2〜3日）

**信頼性・AI品質**
- ジョブdocID=trigger化でcreate()冪等に（起票系の競合を構造で消す）／fetchNewのページネーション対応／testAlarmのtry-finally
- Gemini呼び出しの検証: responseSchema強制（intakeのkind enum）・finishReason確認（途切れ検知）・usageMetadata記録（AIコスト台帳）・429バックオフ・monthlyAiReport失敗時も「失敗した」メールを送る（沈黙させない）
- intake/朝メール所見を**gemini-flashへ**（1/10コスト・OCR構造化はflashで十分）／askAIのターン内メモ化
- READABLEにconstruction・policies追加（AIが工事資料・経営方針に答えられない）／schema.mdとの整合検査をhealthに

**health盲点の追加検査（各10行）**: revenue(prop,month)重複＝売上二重計上検知／prop実在検証／日付欠測件数／cashの鮮度

**フロント基盤**
- api()堅牢化（未ログイン・非JSON応答・r.ok）／alert()残8箇所→notify／preconnect+modulepreload全ページ（初期表示短縮）／画像のキャッシュ許可とHTMLのno-cache化（bfcache復活）

**UX（毎朝の主タスクの摩擦）**
- **「稼働中」フィルタの仕様変更: warn（要対応）は常に表示**——現仕様は要対応カードほど隠れる本末転倒
- 消し込み「取消」を詳細パネルへ移動＋完了時notify＋5秒Undo／未ログイン空状態に「Googleでログイン」ボタン（右上アイコンだけでは伝わらない）／AI質問窓の失敗時に質問文復元＋再送ボタン／「保存しました（モック）」文言の除去

## P2 — 順次（カードスレッドの番・四半期）

bootData移行（三段描画の解消・1カード15分×18）／topbar+認証UIの共通化（initAuthUI）／消し込みの楽観更新（1件ごとの全再取得をやめる）／ナビ改名（財務→月次収支・費用→光熱費・固定費・chat→ゲストチャット等）＋null項目は遷移せず「準備中」表示／a11y最低限（:focus-visible共通・行のtabindex・ドットのaria）／esc統一（niwaの弱体版是正）／os.css末尾の上書き群を基底へ畳む／PROP_LABEL等の和名表をproperties.labelから導出

## AI連動・クラウド連動の深化（発注者追加質問への回答）

**AI**: ①段B操作の下書き（承認済みメニュー残り） ②段F判断文脈（policies/判定を質問窓へ・1時間） ③responseSchema/flash化（上記P1と同件） ④音声入口——現地で「しゃべって投げる」（音声メモ→ai.yamada@→文字起こし→取込。intakeにaudio対応を足すだけ・半日）
**クラウド**: ①Error Reporting集約（無料枠・例外がログに埋まる現状の解消） ②GCP予算アラート（5分） ③Cloud TasksでのGemini再試行はP1の冪等化が済めば不要（再配信で足りる） ④BigQueryは引き続き保留（既定）

## 実行順の提案

1. **P0の8件**（1.5〜2日・大半が共有部/サーバ＝このスレッドで完結。#7のみメンテカード1行）
2. P1を1週間内で消化（UXの稼働中フィルタとログイン導線を先頭に）
3. P2は指示書化して各カードスレッドへ

※セキュリティ#5（トークン）は修正順序に注意: 新方式デプロイ→新トークン発行→業者へ新URL通知→旧トークン無効化。業者のブックマークが一時切れないよう移行期間を1週間置く。
