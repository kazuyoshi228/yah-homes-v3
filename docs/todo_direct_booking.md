# 直販サイト 構築ToDo（優先順）

作成 2026-08-11（同日更新・Beds24検証を反映） / 対象: yah.homes 直販P1 / 正本: docs/design_booking_p1_v5.md
状態の凡例: ☐ 未着手 ／ ◐ 一部済み ／ 決 判断待ち（作業より判断が先）

## P0 — 本番公開の前に必ず（これが終わるまで公開しない）

公開手順は A→B→C→D の順（2026-08-11 合意の段取り）。

**A. Beds24 運用の確定（進行中）**
- ◉ **オートアクション二重送信の解消**（清川 493540/493541 を指名リスト化。再テストでメール不着を確認・2026-08-12 完了）
- ◉ **同・高砂**（517349/541166 を指名リスト化。Airstar 対応済み・2026-08-14 発注者確認）
  ※ Beds24 のオートアクション設定は API から参照できない（該当エンドポイントが500）。
    こちらでの再テストは未実施。直販の通しテスト時に「メールが1通だけ届く」ことで確認する。
- ☐ **Airstar の返信欄テスト**（テスト予約 91284328 のスレッドに一言返信をもらう）→ APIで取得確認
- ◐ **contact@ に落ちる経路を塞ぐ**（2026-08-14 再整理・当面は現状維持の判断）
  予約4通（確定・チェックイン案内・チェックアウト・キャンセル）は no-reply 化＋
  本文を My Page のメッセージへ誘導して解消済み。残るのは次の2本だけ:
  - 問い合わせフォームの自動返信（Reply-To が contact@。本文で返信を促している）
  - レビュー依頼の mailto 宛先（MAIL_FROM = contact@ 固定。★入りの返信がここへ届く）
  → どちらも **Reply-To / mailto の宛先を CONTACT_NOTIFY_TO に変えるだけ**で閉じる。
    Workspace の転送設定は不要になる。未確認: CONTACT_NOTIFY_TO を誰が見ているか、
    レビューの★もそこへ流してよいか（宛先が個人なら通知が増える）。
  ※ フォームの運営向け通知は Reply-To がお客様のアドレスなので、そのまま返信すれば届く。
    この経路は元から contact@ の監視に依存していない。
- ☐ **高砂の運用実態の確認**（自動送信なし＝手動運用？ 直販開始後に高砂は誰が何を送るか）
- ☐ Airstar への一報（オートアクション変更の件・今後OTAを増やす際は指名リストへの追加が必要）
- ◐ **当日予約の締めを確認**（Airstar 回答: 予約当日0時が締め＝前日23:59まで可・2026-08-14）
  未確認2点 → docs/mail_airstar_cutoff_202608.md（未送信）
  ① 高砂だけ Cut off hour が 24（清川は 0）。意図的か・清川に揃えてよいか
  ② **前日12:00のオートアクション後に入った予約に入室案内が届かない**。
     前日23:59まで受けるならOTA側にも同じ穴がある。実運用（手動送信？）の確認が要る

**B. Stripe 本番化（あなたの作業2箇所＋実カード1件）**
- ☐ **本番モードの有効化状態を確認**（未申請なら審査日数がクリティカルパス。今日できる）
- ☐ 本番キー発行 → Secret Manager・クライアント差し替え／本番Webhook登録（実装側）
- ☐ **実カードで1件の通しテスト**＝本番棟の初予約（確定メール→Beds24反映→OTAカレンダー閉塞→即セルフキャンセル→返金→在庫解放。v5 §9）

**C. 本番公開（③本番デプロイ・要承認）**
- ☐ release/book-p1-ms2 を main に統合（予約ページ・ナビ・特商法改定が本番に乗る、一番大きなスイッチ）
- ☐ BOOK_PREVIEW ゲート解除ビルド → デプロイ → 5言語で導線確認
- ☐ **静かに開ける**: 公開初日は導線露出なしで通しテスト、翌日以降にHERO/ナビの予約導線を開く

**D. 公開前に強く推奨**
- ☐ **キーボックス暗証番号の変更 ＋ 旧Google Sitesの公開停止**
  旧ページに 2345 が掲載されたまま。前日メールが番号を直接載せる設計になったため公開前に必須級。
  ①/admin/secrets で棟ごとに別番号へ → ②旧Sitesを非公開に。滞在中・当日到着のゲストがいない日に。

## P1 — 公開直後の運用が回るために

- ◐ **直販ゲストとのメッセージをBeds24に集約**（2026-08-11 実測で成立を確認）
  実測結果: メッセージAPIは直販（API）予約でも双方向に使え、**Beds24受信箱にAirbnbと同列で表示される**
  （Wikiの「OTA専用」記載は誤り）。内部メモは予約のInfoタブに表示。
  残り: ①contact@ 宛メールを受信箱へ自動転記する同期ジョブ ②Airstarの返信をメール配達するジョブ（返信欄テスト待ち）
  つなぎ: contact@ → Airstar Gmail への転送（A項）で先に運用開始。
- ☐ **Beds24 予約Webhookの受け口実装**（発見: 清川に https://www.yah.homes/api/webhooks/beds24 が設定済みだが受け口が実在しない）
  実装すればOTA・直販の予約/変更をリアルタイム取得できる。メッセージ同期の基盤にもなる。
- ☐ **予約成立・キャンセル時のオーナー/運営通知の宛先確認**
  notifyBookings フラグの宛先に運営会社を追加するか（台帳で設定するだけ・要判断）。
- ☐ **Beds24側で手動キャンセルされた場合の同期**
  運営会社がBeds24上でキャンセルすると、Firestore予約・Stripe・在庫ロックが残る。
  案: 定期ジョブでBeds24の状態を照合し、差分をオーナーへ通知（自動返金はしない）。
- ☐ **サーバー側の過去日付ガード**
  見積り・予約APIが過去日を弾くことをサーバー側でも保証（現在はUI側のみ）。
- ☐ **領収書（Receipt）**
  Stripeの領収書メール送信を有効化するだけで足りるか確認 → 有効化。
  宿泊税・インボイスの扱いは税理士確認のうえ文言追加。

## P2 — 立ち上がり後の改善

- ☐ **署名付きチェックインURL**（入室案内のトークン化）
  現在の /how-to/ は「URLを知っていれば誰でも見られる」＝旧Google Sitesと同水準。
  予約ごとの署名付きURLにして、暗証番号の露出を予約者に限定する（v5 §9）。
- ☐ **暗証番号の定期ローテーション運用**
  月次で変更（/admin/secrets）。変更日はチェックイン前日メール送信（10:00）より前の朝に。
- ☐ **purchase計測の重複検証**（GA4）
  実予約データが数件たまった時点で、client/server の二重計上がないか確認。
- ☐ **リピートゲストの認識**
  同一メールの再予約を検知して確定メールの文面を変える／将来の直販割引の下地。
- ☐ **`/admin/*` の X-Frame-Options: DENY**（クリックジャッキング対策・hosting設定1行）
- ☐ **メッセージ機能**（/operators/menu で「準備中」の項目）
  直販ゲストとのやりとりをBeds24に寄せるか、自前で持つかの判断が先。P1の転送運用の結果を見て決める。

## P3 — 資産化・成長

- ☐ **直販フィードバックの蓄積**（レビュー返信メールの内容をFirestoreへ手動転記 or 自動取り込み）
  現在はメール返信のまま。件数が増えたら docs/design_reviews_page.md（v2・保留中）を再検討。
- ☐ **ガイド記事の本文SSR強化**・レビュー節の読み先統一（property_facts へ。4.77直書きの掃除）
- ☐ **週次定点の自動化**（ドラフト保管中・当面は手動の方針）
- ☐ **ko/zh/th メール文面のネイティブ確認**（機械翻訳ベースの訳を実務者チェック）

## 済（この2週間で完了したもの・記録）

- ◉ 予約フロー一式（空室検索→見積り→Stripe与信→Beds24書込→確定メール）＋完了ページ
- ◉ 二重予約防止（Firestore在庫ロック）／返金時のBeds24取消・在庫解放
- ◉ セルフキャンセル（My Page）／キャンセル確認メール
- ◉ 自動メール4通＋キャンセル確認のSSoT化（/admin/templates・5言語・実物プレビュー・テスト送信）
- ◉ チェックイン前日メールに暗証番号の黒カード（/admin/secrets が正）
- ◉ レビュー依頼を返信完結型に（星評価テンプレート＋mailto）
- ◉ 入室案内 /how-to/ 2ページ（5言語・本番公開済み）／guides 64ページの404復旧
- ◉ 管理画面: 予約管理・受信箱・定型メール・鍵番号（オーナー専用）・実物プレビュー・運営メニュー
- ◉ メール翻訳の全言語チェックと修正27件（en/ko/zh/th・4通×検証2段）／英語の単複補正
- ◉ Beds24メッセージ連携の実測（直販予約で双方向API成立・受信箱表示を確認・返信欄はAirstar待ち）
- ◉ 清川オートアクション二重送信の発見と対処（実メール受信で実証 → OTA指名リスト化）
- ◉ Airstar運用の解明: 送信方法スマート（OTA=チャネル/直販=メール）・返信先 zeal.aspiration.partner@gmail.com・高砂は自動送信なし
