# 直接予約基盤 P1 設計書 — 「店構えは自社・エンジンはBeds24」

> 作成: 2026-07-14（magazine側セッションでの合意事項を移植・自己完結）
> 対象リポジトリ: yah.homes-v2（Astro + Firebase Hosting/Functions/Firestore）
> ステータス: **設計確定・実装前**。§14「要確認リスト」だけ発注者確認が残っている。

---

## 1. 背景と診断（2026-07-14 実サイト調査で確定した事実）

- 現行 yah.homes の予約ページ（/booking/kiyokawa/ 等）は **Beds24のホスト型予約画面を iframe 埋め込み**している:
  `<iframe src="https://beds24.com/booking2.php?propid=278158&referer=iframe">`
- これが「遅い・スマホで崩れる・デザインが浮く」の直接原因。**API連携は現状存在しない**（画面の間借りのみ）。
- **iframe内で完結する予約はGA4/Google Adsから計測不能**。現在Google広告のCVは「Airbnbリンククリック」を代用している（2026-07-14時点、日本向けキャンペーン稼働中: CTR 12.8%・CPC約¥91）。
- サイトは Astro 製・多言語パスあり（/ja /ko /zh /th ＋ 既定en）・GA4 導入済み（G-VJ5DDRML79）。
- 物件: 清川（最大7名・寝室3・浴室1階・那珂川沿い・専用駐車場・★4.77/47件）・高砂（最大6名・寝室3・洗面台3・渡辺通駅徒歩5-10分・大型車可・★4.67/36件）。**今後5棟へ拡大予定**。
- 予約チャネル: Airbnb（メイン）＋ Booking.com。チャネル同期は Beds24。

## 2. 決定事項（アーキテクチャ原則）

1. **在庫・料金の唯一の真実 = Beds24**。チャネルマネージャ（OTA同期・ダブルブッキング防止）は借り続ける。フルスクラッチはしない（30棟未満では投資対効果が合わない）。
2. **予約UI・決済・顧客データ・計測 = 自社**。iframeを撤去し、自社画面から Beds24 API v2 を叩く。
3. **運営会社の業務は一切変えない**。自社サイト経由の予約は Beds24 上で他OTAの予約と同じ見え方になる。
4. **顧客データは Firestore に全チャネル分ミラー**（Beds24 Webhook 経由）。これはUI改修と独立して即着手可能。
5. **段階導入（§4）**。各段は次の段の土台になり、捨てる作業がない。ロールバックは常に「前の段に戻す」だけ。

## 2.5 事業KPI（このプロジェクトが何のカネを取りに行くか）

- **直販比率**: 現状ほぼ0〜10%（広告経由もAirbnbリンク送客のため）→ **12ヶ月で35% → 18ヶ月で50%**。
  OTA実効手数料 11.25%（75%×15%）→ 7.5% ＝ **5棟85%巡航で年+287万円**（初年度モデルで+191万）。
- 直販比率の積み上げ計画: ①広告の着地を自社予約へ（MS4・寄与+17〜25%）②リピーター直販クーポン（D1メール・+8〜12%）③指名検索の刈り取り（+3〜5%）④室内QRカード（+2〜3%）。
- **直販ベストレート原則**: 直販が常に最安 or 特典付き（OTA比5%安／レイトチェックアウト等）。
  Booking.comのレートパリティは「割引コード・会員価格・特典」形式で実務対応。
- **OTAは敵にしない**: OTA＝新規獲得の広告費15%と再定義。新規はOTAで獲り、2回目から直販へ。
  Airbnbランキング（新規の蛇口）を痛める撤退戦はしない。
- 運営体制の決定事項（2026-07-14）: **現管理会社は維持**（12%は相場15〜20%比で良心的・関係良好）。
  5棟化の契約時に「定型連絡の自社自動化（§9）＋5棟全委託」と引き換えに**10%へのスコープ交渉**を行う。
  分解外注（メッセージ代行＋セコム＋清掃直契約）は実行案ではなく**BATNA（交渉の物差し・有事のバックアップ）**として見積もりのみ取得。

## 3. ページ構造（IA）—「売るページは棟ごと・予約は1枚」（ホテル型）

```
 Home（/）            玄関: ブランド・棟の顔見せ・日付×人数検索ボックス
   ├ /properties/kiyokawa/  ② 物件詳細 =「売る」ページ（棟ごと・軽さ最優先）
   ├ /properties/takasago/     写真・設備・レビュー・地図・周辺 ＋ ミニ予約ウィジェットのみ
   │      │ CTA（棟・日付を引き継いで遷移）
   │      ▼
   └ /book/           ③ 予約ページ =「確定する」ページ（全棟共通・1枚だけ）
                         日付×人数 → 泊まれる棟をホテルの部屋選択のように一覧
                         （清川 ¥XX,XXX総額 / 高砂 ¥XX,XXX総額 / 満室棟は明示）
                         → 棟を選んで確定へ（自社決済・MS3）
```

- **予約フローは全棟で /book/ の1枚に統合**（ホテルの「部屋を選ぶ」モデル）。棟が5に増えても /book/ は無変更で、②のページを足すだけ。
- **クロスセルが構造で効く**: 目当ての棟が満室でも、その場で他棟の空きが見える（棟別予約ページでは離脱していた客を拾う）。
- ②からの遷移は `/book/?prop={slug}&checkin=…&checkout=…&guests=…` で**棟選択済み・日付入力済み**の状態で開く（選び直しの手間ゼロ）。
- **②は1ページ1仕事**: 売ることに専念。フル予約フォームは置かない。SEO/広告の着地は必ず②。
- 既存 `/booking/{slug}/` は `/properties/{slug}/` へ**301リダイレクト**（URL意味論の修正。「book」は③に譲る）。
- ※旧案にあった「① 横断検索ページ」は /book/ に吸収（検索＝予約入口として1枚で兼ねる）。
- ②のミニ予約ウィジェット仕様:
  - PC: 右カラム sticky カード（料金〜/泊・チェックイン/アウト・人数・総額・CTA）
  - スマホ: 画面下部固定バー（「¥XX,XXX〜 [空室を見る]」）
  - カレンダーJSは**ユーザーが日付欄に触れてから遅延ロード**（②の初期表示速度を犠牲にしない）

## 3.5 ストアフロントUXパターン集（大手OTAから真似るもの・避けるもの）

②物件詳細・③/book/ の実装時に適用する。優先度順（★=最優先・実装が軽くCVR直結）。

### 採用するパターン

1. ★**総額表示**: 「¥45,000/泊」でなく「2泊6名 合計¥98,000（清掃料込み）」を最初から表示。内訳（泊数×単価＋清掃料＋税）は畳んで開示。手数料の後出しは絶対にしない。**直販の総額先出しはOTAへの差別化そのもの**。
2. ★**キャンセルポリシーの1行明示**: 料金直下に「◯月◯日まで無料キャンセル」を日付入り1行で。全文はリンク先。家族客の最後のためらい対策。
3. **寝室ごとのベッド構成カード**（Airbnb: Sleeping arrangements）: 「寝室1: ダブル1／寝室2: シングル2…」を部屋カードで。6〜7名グループ・三世代客は「誰がどこで寝るか」で決める。部屋割りの提案例（3世代なら…）まで書く。
4. **カテゴリ別レビュー＋Airbnbレビュー転載**: 総合★だけでなく内訳（清潔さ/立地/チェックイン）バー＋レビュー本文（国・時期つき）を各棟10件以上。直販の「ここ大丈夫？」をAirbnbで積んだ資産（★4.77/47・★4.67/36）で潰す。
5. **部屋ラベル付き写真ギャラリー**（Vrbo: 部屋別ツアー）: 「リビング/寝室1/浴室/駐車場」でセクション分け。洗面台3（高砂）等の武器は写真で探せる状態に。
6. **設備の構造化リスト＋家族向けバッジ**: ベビーベッド・ベッドガード・子ども椅子・洗濯乾燥機を独立セクションに（全市場で家族系クエリがYoY∞成長の実査結果に対応）。同内容を VacationRental schema に焼き込み GEO にも効かせる。
7. **アカウント不要のゲスト予約**（MS3）: Airbnbは登録必須＝大手が構造的にできない差別化。メールだけで予約完了させる。
8. 余裕があれば: 地図＋「ここから何分」（柳橋7分等のコーパス流用）／総額の通貨並記（≈$・₩・NT$）／Self check-in バッジ／「直販が一番お得」比較表（OTA vs 直販: 価格・特典・柔軟性）。

### 避けるパターン（大手の悪癖）

- ❌ 偽の緊急性（「今20人が見ています」・常時「残り1室」）。**本当に残り1棟の時だけ**表示するのは可。虚偽表示はレビュー資産を毀損する。
- ❌ 会員登録の強制・ログイン誘導。
- ❌ クロスセル物量（保険・レンタカー等）。1ページ1仕事に反する。
- ❌ 手数料の後出し（採用パターン1の逆）。

## 4. 実装計画（2026-07-14再改訂: 単一フェーズ・4マイルストーン。暫定対応は全廃）

トラフィックが立ち上がる前のため、暫定つなぎ（旧段0の直リンク化・旧段1のホストページ化粧）は**実施しない**。
既存のiframeは /book/ 完成時のカットオーバーまで放置し、一発で本物に差し替える。

| MS | 内容 | 目安 | Done定義 |
|---|---|---|---|
| **MS1 疎通** | APIトークン接続・物件構成の実データ確認・**Webhook→Firestoreミラー稼働** | 1〜3日 | 全チャネルの予約がFirestoreに入り始める（＝API統合の実証込み） |
| **MS2 表示** | /book/ のUI＋空室/料金API（§7-1/7-2手順1-2）＋②のミニウィジェット | 1週 | 日付×人数→泊まれる棟と総額が即表示 |
| **MS3 決済** | Stripe＋POST /bookings＋失敗時自動返金（§7-2手順3-5） | 1〜2週 | テスト予約がBeds24に立ち、決済まで完結 |
| **MS4 カットオーバー** | iframe撤去→/book/へ差し替え・/properties/ 改称と301・広告の着地差し替え | 1〜2日 | 全導線が自社予約に切替（Beds24ページはフォールバック温存）・**/book/ の認証解除と noindex 除去を確認**（§7-3） |

- 並行準備（今日から可能）: Stripeアカウント開設・特商法表示ページ・自動メールテンプレ4通×5言語。
- **Webhookミラー（MS1）はサイト訪問者ゼロでも価値がある**: Airbnb/Booking経由を含む全予約・全顧客が初日から自社DBに蓄積される。

## 5. MS1 仕様 — 基盤整備

### 5-1. iframe撤去（MS4カットオーバー時に実施）
- `/booking/{slug}/` の iframe を /book/ への導線に差し替え（暫定リンク化はしない・§4参照）。
- Airbnbリンクは併置のまま（当面の信頼担保・比較手段）。

### 5-2. Webhook → Firestore ミラー（MS1の中核）
- Beds24 管理画面で Webhook を設定（予約の作成/変更/キャンセル時に通知）。
- 受け口: `functions` に HTTPS 関数 `beds24Webhook` を追加。
  - 検証: 共有シークレット（クエリ or ヘッダ）で照合。不一致は 403。
  - 受信後、Beds24 API `GET /bookings/{id}` で正データを取り直してから保存（Webhookペイロードを直接信用しない）。
- Firestore スキーマ:

```
bookings/{beds24BookingId}
  propKey        "kiyokawa" | "takasago" | …
  channel        "direct" | "airbnb" | "booking" | …
  status         confirmed | cancelled | modified …（Beds24のstatusを写像）
  checkIn/checkOut  ISO date
  numAdult/numChild
  amount         合計金額（数値・JPY）
  guest: { name, email, phone, country, lang }
  createdAt/updatedAt  epoch ms
  raw            Beds24レスポンス原文（スキーマ変化に備え保全）

guests/{emailLower}          ← 顧客台帳（リピート判定の主キーはemail小文字）
  name, country, lang
  bookingIds: [ ... ]
  firstStay / lastStay / totalStays / totalAmount

events/{auto}                ← 既存の計測系と整合させる
```

- firestore.rules: `bookings` `guests` は**クライアント読み書き禁止**（Functionsのみ・admin経由）。個人情報のため公開読み取りは絶対に付けない。

## 6. 【廃止】旧段1（Beds24ホストページのブランド化・GA4）

**廃止**。捨てる資産への投資となるため実施しない。Beds24ホストページは無加工のまま「障害時フォールバック」としてのみ温存。

## 7. MS2〜3 仕様 — /book/ フル実装

### 7-1. 読み取りAPI（Functions プロキシ）
- Functions に `bookingApi`（HTTPS）を追加。クライアントは自社ドメインの同関数のみ叩く（**BEDS24トークンをクライアントに出さない**）。
- エンドポイント（自社側）:
  - `GET /api/availability?prop={slug}&from=&to=` → 日別の空/塞・最低泊数
  - `GET /api/quote?prop={slug}&checkin=&checkout=&adults=&children=` → 総額・内訳
- Beds24 API v2 を利用（token認証）。代表エンドポイントは在庫カレンダー系・料金オファー系・予約系（**正確なパス/パラメータは実装時に公式 API v2 ドキュメントで最終確認**。v1とv2で体系が違うため注意）。
- キャッシュ: 空室・料金は **5分キャッシュ**（Functionsメモリ＋`Cache-Control`）。予約直前の正はBeds24側で担保されるため表示用途は5分で実用十分。
- レート制限対策: 呼び出しはFunctions集約＋キャッシュで吸収。429時は指数バックオフ。

### 7-2. /book/（自社・全棟共通の予約ページ）— 予約完結までのフルフロー

**設計原則: とにかくミニマル（2026-07-14 発注者決定）。** ③は「確定する」ページ — 置くのは
日付×人数・棟カード（写真1枚+総額+定員）・ゲスト情報・決済のみ。ヒーロー・レビュー・設備紹介・
FAQ・関連リンクは置かない（売り込みは全部②の仕事）。ナビは「ロゴ＋戻る」だけの決済モードに簡素化。
1. 日付×人数フォーム（②から来た場合はURLパラメータで入力済み・棟も選択済み表示）
2. `/api/availability`＋`/api/quote` を**全棟並列**で叩き、泊まれる棟をカード一覧（総額・定員・満室明示・§3.5の総額表示原則）
3. 棟を選ぶ → 予約内容確認 → ゲスト情報入力（宿泊者名簿要件込み・**アカウント登録不要**）→ Stripe決済
4. 決済フロー: Stripe PaymentIntent（オーソリ）→ 成功後 `POST /bookings`（Beds24へ書き込み）→ **書込成功で初めてキャプチャ・確定** → 確認メール＋Firestoreミラー
5. 書込失敗（OTAとの同時性衝突・レアケース）: 自動でオーソリ解放（返金）＋代替日提案画面。**このフォールバックは必須実装**
- 暫定リンク運用は行わない（§4）。物件構成（同一prop内2部屋か別propか）はMS1の実データ確認で確定し、/api/availability の実装に反映する（§14-2）。

### 7-3. 公開までのアクセス制御（2段構え・2026-07-14追記）

実装し切るまで /book/ を一般公開しない。段階でガードを使い分ける:

1. **開発中（MS2の大半）— プレビューチャンネル方式（推奨・認証コード不要）**
   - 本番ビルドでは /book/ を**生成しない**（環境変数ゲート `BOOK_PREVIEW=1` のときのみ生成。guides の GUIDES_PREVIEW と同じ手口）。
   - 確認は Firebase Hosting preview channel（推測不能URL）へのデプロイで行う。
   - 本番は404・sitemapにも載らない＝SEO事故ゼロ。
2. **本番ドメインでの実機テスト（MS3の決済テスト頃）— Basic認証**
   - どのみち作る `bookingApi`（Cloud Function）に /book/ の rewrite を向け、関数側で **Basic認証（401）** を挟む。
   - Stripeテスト決済・スマホ実機の挙動を本番URL・本番構成のまま関係者だけで確認できる。
   - 保護中は **noindex を併記**（401でもURLだけ拾われるのを防ぐ）。
3. **カットオーバー（MS4）** — 認証解除・noindex除去→公開→広告/guides handoff差し替え。

⚠️ MS4 の Done 定義に「**noindex 除去の確認**」を追加（§4の表と併せて読む）。

## 8. 【統合済み】旧段3

旧段3（Stripe決済・POST /bookings）は §7-2 の手順3〜5（MS3）に統合した。「着手条件」も撤廃 — 最初から実装する。
経済根拠: 直予約1件はOTA手数料約15%（客単価5万円× 平均2泊=予約10万円で約¥15,000）を回収する。

## 9. ゲスト連絡（運営会社の業務設計）

- 直予約ゲストはAirbnbメッセージに存在しない → **Beds24 の自動アクション（Auto Actions）でメール自動送信**に統一。新ツール導入なし。
- テンプレ4通 × 5言語（ja/en/ko/zh-TW/th）:
  1. 予約確認（即時）: 内容・総額・キャンセルポリシー
  2. チェックイン案内（3日前）: 住所・鍵・手順・駐車場（清川）/駅からの道順（高砂）
  3. 当日リマインド: 緊急連絡先
  4. チェックアウト後（翌日）: お礼＋レビュー依頼＋**次回直予約クーポン**
- **確認済み（2026-07-19）: 現行はパターンA（Airbnbアプリ手動）**。運営会社への聞き取りで確定。
  - 含意①: Beds24 Auto Actions は未使用 → **直予約向けメール自動化はゼロから設定**（MS3並行タスク。テンプレ4通×5言語を私が起案→Beds24に登録）。
  - 含意②: OTAゲストの連絡は現行のまま変えない（Airbnbアプリ手動を継続）＝「運営会社の業務は一切変えない」原則と整合。直予約ゲストだけがBeds24自動メールを受ける二本立て。
  - 含意③: 将来5棟化・直販比率上昇時に、OTA側もAuto Actionsへ寄せる提案余地あり（管理会社の工数削減＝10%スコープ交渉の材料）。

### 9-2. 対応の3層モデルとエスカレーション（クレーム・緊急時）

怒っているゲスト・深夜緊急は3層で処理する（例: 深夜「お湯が出ない」）:

| 層 | 仕事 | 担当 |
|---|---|---|
| ①コミュニケーション | 一次対応・謝罪・聞き取り・経過連絡・翌朝フォロー | 管理会社（現体制）／将来はメッセージ代行も可 |
| ②物理対応 | 現地駆けつけ・解錠・設備の一次確認・業者手配 | 管理会社（現体制） |
| ③経営判断 | 返金・補償額・退去・警察 | オーナー。ただし下のルール化で月1件程度まで削減 |

**エスカレーション表（事前権限移譲・運用開始時に管理会社と合意）**:

```
・設備不良で入浴不可 → 銭湯代 ¥2,000×人数まで現場判断で即補償OK
・近隣からの騒音クレーム → 1回目は現場が注意連絡 → 改善なければオーナーへ
・¥10,000 までの補償・部分返金 → 現場判断OK（月次報告）
・全額返金・退去・警察・けが人 → 即時オーナーへ電話
```

軽微な判断を現場に渡すことで、ゲストは「その場で解決」を体験し（レビュー防衛）、
オーナーに届くのは本物の経営判断のみになる。

### 9-3. 運営会社の受け口 — メールボックス・管理画面・紛争分担（2026-08-06追記）

紛争処理・ゲスト対応の一次窓口は運営会社。直販固有の新レーン（決済紛争は当社Stripeに来る・直販ゲストはAirbnbメッセージに居ない）に受け口を用意する:

1. **専用メールボックス `bookings@yah.homes`**（mail.yah.homes に追加・転送先=運営会社担当＋オーナー）。§9のBeds24自動メール4通はすべて replyTo=bookings@ とし、ゲスト返信が運営会社に直接届く構造にする。オーナーは同報で見えるだけ（一次対応しない＝§9-2の3層モデルと整合）。
2. **/admin/bookings**: partnersAdmin と同型（Googleログイン＋許可メールリスト）。許可リストに運営会社担当を追加。機能=予約一覧・ゲスト連絡先・対応メモ（誰が何を対応したかの台帳）。**返金など金銭操作はオーナー権限に限定**（§9-2の①②=運営会社・③=オーナーの画面版）。
3. **Stripe紛争（チャージバック）**: 通知→オーナー＋bookings@へ転送→運営会社が宿泊事実・チェックイン記録・現地対応履歴を反証材料として返す→オーナーがStripeへ提出。役割=**事実確認は運営会社・金銭処理はオーナー**。
4. **通知**: 直販の新規/キャンセル時に bookings@ へも通知（件名に「直販」明示。運営会社はBeds24でも見えるが、直販レーンだけメールでも区別できるように）。

### 9-4. 宿泊者名簿（旅館業法・法定）— 管理主体は運営会社（2026-08-06追記）

旅館業許可は運営会社名義（リスティングも先方名義）のため、**名簿の法的義務者は運営会社**。当社は取得代行＋保管インフラの位置づけ:

- 直販予約フォームで名簿事項（氏名・住所・職業・国籍/旅券番号）を取得 → Firestore保存（§5-2どおりクライアント読み書き禁止・admin経由のみ）
- **運営会社への自動連携**: /admin/bookings で閲覧＋チェックイン前に名簿を bookings@ 経由で運営会社へ自動送付（形式は§14の確認事項）
- 保存期間（3年）・削除運用は運営会社の規程に合わせる
- Airbnb経由では Airbnb+運営会社の既存の仕組みで賄っている情報を、直販でも「同等の情報が同じ宛先に届く」形で再現するのが趣旨

**Airstar合意事項（面談マター・§14に追加)**: ①直販分のゲスト対応・名簿管理を現行運用費内で受けるか件数課金か ②名簿の受け渡し形式（admin閲覧/メール/月次CSV）③bookings@ の転送先アドレス。

## 10. 計測設計

| ファネル | GA4イベント | 備考 |
|---|---|---|
| ②物件詳細 表示 | `view_item`（item_id=slug） | |
| ミニウィジェット操作 | `select_dates` | カスタム |
| ③到達（/book/） | `begin_checkout` | MS2以降 |
| 予約完了 | `purchase`（value=金額, transaction_id=beds24BookingId） | MS3で自社計測（クロスドメイン不要） |

- Google Ads: `purchase` をプライマリCVへ昇格し、既存「Airbnbリンククリック」CVは**セカンダリに降格**（学習信号の二重計上を防ぐ）。
- Webhookミラー（MS1）があるため、**広告→予約の真の突合はFirestore側でも可能**（GA4が欠測してもデータは残る）。

## 11. 受け入れ基準（P1 = MS1〜4完了時）

- [ ] /properties/{slug}/ の LCP がモバイル実測 2.5s 未満（iframe時代より体感即時）
- [ ] ②→③受け渡しで日付・人数が引き継がれる（手入力のやり直しゼロ）
- [ ] Airbnb経由・Booking経由・直予約の全件が Firestore `bookings` に自動記録される
- [ ] GA4 で view_item → begin_checkout → purchase のファネルが切れる
- [ ] 運営会社の作業手順書に変更ゼロ（確認: 運営会社ヒアリング1回）
- [ ] iframe版へ5分で戻せる（旧セクションはコメントアウト保持で当面残す）

## 12. リスクと手当て

| リスク | 手当て |
|---|---|
| Beds24 API仕様の想定違い | §7の該当箇所を実装冒頭に公式docで確定してから着手（半日バッファ） |
| Webhook欠落（通知漏れ） | 夜間バッチで `GET /bookings`（更新日フィルタ）を日次同期し突合 |
| Beds24障害 | ②はキャッシュ表示継続＋CTAをAirbnbリンクへ自動フォールバック |
| 個人情報 | bookings/guestsはFunctions専用アクセス。ルールで全クライアント拒否 |

## 13. 工数・体制目安

- MS1: 1〜3日／MS2: 1週／MS3: 1〜2週／MS4: 1〜2日 — 計2〜4週
- 実装は本リポジトリ（yah.homes-v2）内で完結。外部の新規契約は**Stripeのみ**（並行で開設・特商法ページ整備）。

## 14. 要確認リスト（実装前に発注者が確認）

1. **Beds24のプランとAPIアクセス可否**（API v2利用にはアカウント設定でトークン発行が必要）
2. **Beds24の物件構成: 清川・高砂は同一propid（278158）内の「部屋」か、別propidか**
   — /api/availability の照会形とWebhookの対応付けに使う（MS1で実データからも確認可）。
   どちらでも自社/book/が束ねるため設計への影響は小（/api/availability の照会形が変わるだけ・MS1で確定）
3. ~~ゲスト連絡の現行パターン~~ → **回答済み（2026-07-19）: A（Airbnbアプリ手動）**。§9に含意を記載
4. Beds24 Webhook 機能の設定場所とペイロード形式（プランにより差異）
5. ~~Stripeアカウントの有無・特商法表示~~ → **完了（2026-07-19）: Stripeアカウント取得済み・特商法ページは /legal/tokushoho/ で公開済み**（決済開始時に「直接決済は準備中」の行を更新するだけ）

### 14-6. 電話番号の扱い（2026-07-16 決定）

- **会社概要（/about/内）・LodgingBusiness schema には電話番号を載せない**（会社概要は法的に不要、schema telephone は任意）。
- **特商法ページ（MS3で新設）** は「電話番号は請求があれば遅滞なく開示します」方式を採用（通信販売で認められた正規運用）。
  - 条件: 開示請求の窓口（contact@mail.yah.homes / 問い合わせフォーム）が速やかに応答すること。既存の自動応答（2〜3営業日）で担保。
- 登記情報（社名・代表・所在地・法人番号・創業日・事業内容）は /about/ の会社概要に掲載済み（NAP一貫性の基点）。
